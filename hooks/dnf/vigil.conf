# /etc/dnf/plugins/vigil.conf — enables hooks/dnf/vigil.py.
# Installed by the vigil-baseline RPM. Disable per-host by setting
# enabled=0 here, or globally by `dnf --disableplugin=vigil ...`.
[main]
enabled=1
