DPkg::Pre-Invoke { "if [ -x /usr/bin/vigil ]; then /usr/bin/vigil maintenance enter --quiet 2>/dev/null; fi; true"; };
DPkg::Post-Invoke { "if [ ! -x /usr/bin/vigil ]; then logger -t vigil-apt 'vigil binary not found; skipping refresh'; true; elif /usr/bin/vigil baseline refresh --quiet 2>/dev/null; then /usr/bin/vigil maintenance exit --quiet 2>/dev/null; true; else logger -t vigil-apt 'baseline refresh failed; investigate with: vigil doctor'; notify-send -u critical 'Vigil' 'Baseline refresh failed after package transaction. Run vigil doctor to investigate.' 2>/dev/null; /usr/bin/vigil maintenance exit --quiet 2>/dev/null; true; fi"; };
